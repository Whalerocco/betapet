/**
 * Spanish equivalent of allowedAbbreviations.ts: explicit abbreviation exceptions
 * (dictionary.md section 16): starts empty. Do not add entries here casually — every exception
 * should be a deliberate project decision with a documented reason (see docs/decisions.md), not
 * added just because an abbreviation is common in everyday Spanish.
 */
export const ALLOWED_ABBREVIATIONS_ES: ReadonlySet<string> = new Set([]);
