/**
 * Explicit abbreviation exceptions (dictionary.md section 16): starts empty. Do not add entries
 * here casually — every exception should be a deliberate project decision with a documented
 * reason (see docs/decisions.md), not added just because an abbreviation is common in everyday
 * Swedish.
 */
export const ALLOWED_ABBREVIATIONS: ReadonlySet<string> = new Set([]);
