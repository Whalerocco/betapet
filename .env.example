# Neon Postgres, Europe (Frankfurt) — DEC-020.
# Use the pooled connection string from the Neon dashboard.
DATABASE_URL="postgresql://user:password@ep-example-pooler.eu-central-1.aws.neon.tech/betapet?sslmode=require"

# Better Auth (DEC-020). Generate a secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"
